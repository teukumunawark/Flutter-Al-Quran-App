import 'package:flutter/material.dart';

class ListDetailCard extends StatelessWidget {
  const ListDetailCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
